# Plans

Active execution plans (YAML), consumed by the daemon's planner /
executor. Inactive plans go in [`docs/plans/archive/`](./archive/).

The schema is documented in `crates/convergio-planner/README.md` once
Layer 4 is implemented.
