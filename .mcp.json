{
  "mcpServers": {
    "convergio": {
      "type": "stdio",
      "command": "convergio-mcp",
      "args": ["--url", "http://127.0.0.1:8420"],
      "description": "Convergio local MCP bridge. Exposes convergio.help and convergio.act."
    },
    "context7": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@upstash/context7-mcp"],
      "description": "Live documentation fetcher for libraries / frameworks / SDKs."
    }
  }
}
