# convergio-thor

Layer 4 (reference) — validator agent.

**Status: skeleton.** See [ROADMAP.md](../../ROADMAP.md) week 5-6.

The validator is the last gate before a plan is marked `completed`. It
runs as a Layer 3 process — its independence is the point. Replace it
with your own validator if your domain (healthcare, finance, ...)
needs custom checks.
